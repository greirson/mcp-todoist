# Todoist API Token (REQUIRED)
# Get your token from: https://todoist.com/app/settings/integrations
TODOIST_API_TOKEN=your_todoist_api_token_here

# MCP Authentication Token (REQUIRED for Railway deployment)
# Generate a strong random token for HTTP/SSE endpoint security
# Example: openssl rand -base64 32
MCP_AUTH_TOKEN=your_secure_random_token_here

# Server Port (Optional - Railway sets this automatically)
# Default: 3000
# PORT=3000

# Dry-Run Mode (Optional)
# Set to "true" to simulate operations without making real changes
# Default: false
# DRYRUN=false
